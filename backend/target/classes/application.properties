# Server configuration
server.port=8080

# MongoDB configuration
spring.data.mongodb.uri=mongodb+srv://Isuru4:Isurulakshan4%23@studentmanagementsystem.5e4dds6.mongodb.net/art_cafe?retryWrites=true&w=majority&appName=StudentManagementSystem
spring.data.mongodb.auto-index-creation=true

# Enable logging for MongoDB
logging.level.org.springframework.data.mongodb=DEBUG

# JPA/Hibernate properties
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=artCafeSecretKey_ThisIsAVeryLongAndSecureKeyForJwtSigning_2024_HMACSHA256_MinimumLength32Bytes
jwt.expiration=86400000

# Static resources configuration - simplified and fixed
spring.web.resources.static-locations=classpath:/static/,file:./uploads/
spring.mvc.static-path-pattern=/**

# File upload configuration
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Configure CORS globally for development
spring.webmvc.cors.allowed-origins=*
spring.webmvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.webmvc.cors.allowed-headers=*
spring.webmvc.cors.allow-credentials=true
spring.webmvc.cors.max-age=3600

# DNS Resolution Configuration
# These properties help resolve the DNS issue related to MongoDB Atlas connections
spring.data.mongodb.connect-timeout=30000
spring.data.mongodb.socket-timeout=60000
